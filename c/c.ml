(** {1 Entry Points}

    These modules contain the generated types and functions enabling usage of
    Clipper2 from OCaml. *)

include C_

(** {1 Binding Description Functors}

    Doc comment headers are available to make browsing the bound functions
    easier in {{!module-Clipper_c_function_descriptions.Functions} [Functions]}. *)

module Clipper_c_function_descriptions = Clipper_c_function_descriptions
module Clipper_c_type_descriptions = Clipper_c_type_descriptions
